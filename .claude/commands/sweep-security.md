Use the code-quality-sweep agent to scan this repository for security issues only (Category 4).

Focus on: hardcoded secrets, SQL injection vectors, missing input validation, insecure CORS, dependencies with known CVEs, missing rate limiting, and logging of sensitive data.

Fix what can be safely fixed and flag everything else for manual review.

$ARGUMENTS
